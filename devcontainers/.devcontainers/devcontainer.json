// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/ubuntu
{
	"name": "OpenUniversity",
	// Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile

	"image": "docker.io/ubuntu:rolling",

	// Features to add to the dev container. More info: https://containers.dev/features.
	"features": {
		"ghcr.io/devcontainers/features/python:1": {},
		"ghcr.io/prulloac/devcontainer-features/latex:1": {
			"scheme": "small",
			"packages": "ifmtarg,isodate,setspace,substr,xifthen"
		}
	},

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	// "forwardPorts": [],postCreateCommand

	// Use 'postCreateCommand' to run commands after the container is created.
	"postCreateCommand": "apt-get update && apt-get install -y --no-install-recommends python3-ipykernel python3-pandas",

	// Configure tool-specific properties.
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-toolsai.datawrangler",
				"ms-toolsai.jupyter",
				"neo-ltex.ltex",
				"shardulm94.trailing-spaces"
			],

			"settings": {
				// Ltex settings
				"ltex.enabled": true,
				"ltex.language": "en-GB",
				"ltex.additionalRules.motherTongue": "en-GB",
				// Latex workshop settings
				"latex-workshop.synctex.afterBuild.enabled": true
			}
		}
	},

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "root"

	"mounts": [
		"source=${localEnv:HOME}/texmf,target=/root/texmf,type=bind,consistency=cached,readonly"
	]
}
